# Template for local environment variables.
# Add non-secret configuration here when the app needs build-time or runtime
# environment values, then copy this file to .env. Do not commit .env or secrets.
# Application
APP_ENV=development
API_BASE_URL=https://api.example.com
API_TIMEOUT_MS=15000

# Feature flags
ENABLE_ANALYTICS=false
